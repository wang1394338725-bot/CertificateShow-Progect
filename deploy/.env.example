# 复制本文件为 .env 并填入真实值
# 生产环境务必使用强密码和随机密钥，不要保留示例值

# MySQL root 密码
DB_PASSWORD=change-me-strong-password

# JWT 签名密钥：64 位十六进制
# 生成方法（Linux）：openssl rand -hex 32
JWT_SECRET=change-me-64-hex-chars-random
