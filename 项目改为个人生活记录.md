# 项目改为「个人生活记录」改造评估

> 评估时间：2026-09-17 ｜ 基线：main 分支 ae7c8a8（奖状展示完整版），改造在 personal-life-records 分支进行

## 总体结论

**可行，且成本比预想的低。** 数据库可以零改动，后端接口可以零/极小改动，工作量集中在 4 个前端文件的文案与布局调整。但有 1 个安全问题必须先决策（隐私），1 个字段选择需要拍板（附录存哪），详见下文。

## 你的方案逐条评估

| # | 你的想法 | 可行性 | 说明 |
|---|---------|--------|------|
| 1 | 前后端变量和数据库字段一个不改 | ✅ 可行 | 后端实体/接口全部保持原样 |
| 2 | 前端只换展示名称 | ✅ 可行 | HomeView / PublicBrowseView 文案替换即可 |
| 3 | 「最新荣誉」→「最新记录」、「等级前三」→「其他记录」 | ✅ 可行 | 两个 section 复用现有 /view 接口，换排序参数（awardDate desc / 其他维度），后端不用动 |
| 4 | 详情只留一个 char 字段存吐槽/附录 | ⚠️ 需拍板 | 实体没有专门 remark 字段，但有两个空闲 String 可复用：`organization`（颁证机构）和 `projectName`（所属项目）。推荐复用 `organization`——零 DB 改动，旧数据该字段基本为空。另一方案是新增 remark 列（需动 entity + init.sql + 服务器一条 ALTER TABLE，语义更清晰） |
| 5 | 主页图片上方显示附录前几个字（或标题） | ✅ 可行 | 建议保留 title 作为「记录标题」：delete_audit 的 certificate_title 快照、/search 关键词匹配、Excel 导入必需列都依赖它，删除成本反而高 |
| 6 | 主页去掉搜索 | ✅ 可行 | 纯前端删搜索组件；/search 接口保留不影响任何功能 |
| 7 | 后端只改上传/查看/修改，其他不显示 | ✅ 大体如此 | 已核实 UploadDTO/CertificateUpdateDTO **没有任何 @NotBlank 强制校验**，前端删掉字段后端直接接得住；唯一注意 awardLevel 前端隐藏后建议提交固定默认值「其他」（业务排序 FIELD(award_level,'国家级',...) 依赖它，空值会排到最末，不会报错） |

## 必须先决策的 1 个安全问题：隐私

当前 `AuthInterceptor` 的 PUBLIC_PATHS 白名单里有 **/home、/view、/search**——意味着**任何人打开你的域名就能看到你上传的全部图片和文字，无需登录**。

奖状展示时期这是特性（公开浏览），换成个人生活记录就是隐私泄露（你的照片、日常吐槽全公开，还能被搜索引擎爬到）。

**建议**：从白名单移除 /home、/view、/search 三条（后端一处改动），全站登录后才可见；前端主页相应改为登录后的私人首页。这是整个改造里最重要的一步，比所有文案改动加起来都重要。

## 可选但建议顺手考虑的

- **Excel 导入导出表头**（奖状名称/获奖成员…）：不改也能用（表头只是匹配别名），要改也只是后端常量字符串
- **虚位以待/置顶逻辑**（isPinned + sortOrder）：可复用为「精选记录」置顶
- **备案变更**：网站名称/内容变更需在阿里云备案控制台提交「变更备案」；个人性质的生活记录属于个人备案合规范围，通常 1~2 周
- **页面标题/浏览器 tab**：index.html 的 `<title>` 与登录页品牌文案

## 改动范围预估（执行时）

| 层 | 文件 | 改动性质 |
|----|------|---------|
| 后端 | AuthInterceptor（白名单） | 若做私有化：删 3 行；否则零改动 |
| 前端 | HomeView.vue | 文案 + section 逻辑 + 隐藏搜索 |
| 前端 | PublicBrowseView.vue（或直接弃用） | 私有化后此页可删或留空壳 |
| 前端 | UploadView.vue | 表单字段精简 + 默认值 + 文案 |
| 前端 | BrowseView.vue | 表格列精简 + 编辑弹窗文案 |
| 数据库 | 无 | 零改动（复用 organization） |
| 服务器 | 重新 build 前端 + scp dist | 流程与之前完全相同 |

## 回滚方式

`git switch main` 即回到奖状展示版（代码）；服务器上若已部署新版 dist，回滚 = 把 main 分支重新 build 的 dist 传上去，数据库数据两版通用（字段结构未变）。
